---
name: codex-quota-resume
description: 设置、更新或停止 Codex 额度恢复后的自动续跑。读取账户五小时和周额度的实际重置时间，在恢复后五分钟检查，并在原任务继续因额度耗尽中断的工作。适用于用户要求额度监控、恢复后自动继续或无人值守续跑；仅询问方案时不创建自动任务。
---

# Codex 额度恢复续跑

将用户授权的任务在额度恢复后接着执行。每次只预约下一次检查，检查时获取新的重置时间，再更新同一自动任务。默认缓冲五分钟，不按小时轮询，不把截图中的时间或日期写死。

## 能力与授权

- 发现并读取当前环境的工具说明。需要账户额度读取、自动任务创建/更新、任务列表、任务历史读取、向原任务发送消息；当前桌面环境通常分别提供 `get_usage_limits`、`automation_update`、`list_threads`、`read_thread`、`send_message_to_thread`。可用时用 `wait_threads` 的即时快照核验。名称前缀和参数以实际工具为准。
- 工具缺失时搜索可用工具；仍缺失则说明缺哪项能力，不声称已设置，不读取认证令牌或猜测私有接口。不依赖模拟鼠标点击监督界面。
- 用户要求启用即授权设置和在约定范围内续跑，不重复索要确认。用户只问可行性或查看状态时只解释或读取。
- 优先使用用户指定任务范围；未指定而明确要求自动发现时，限于当前账户可访问、未归档且有明确额度中断证据的 Codex 任务。续跑不扩大原任务授权，不撤销取消、暂停或等待用户决策的状态。
- 技能提供操作流程，不是独立后台服务。调度由宿主执行，本地任务需要电脑和应用可用。配置保存不等于已验证跨额度恢复；不要承诺关机运行、额度不足时仍能推理或单次计划必然自动续订。

## 初次设置或修改

1. 确认当前时间、用户时区、承载监督的对话以及任务范围。不要从安装目录猜测时区。默认在当前监督对话创建 heartbeat，不为每次检查新建对话。
2. 检查宿主现有自动任务；有同用途、同范围的自动任务时更新它，保留无关字段。若用户明确正在修改某个自动任务，使用其真实 id。不要修改其他用途的自动任务。
3. 读取实际账户额度，按下节计算下次检查时间。未知账户额度与无限额度不是同一种情况。
4. 将运行状态存放在当前监督工作目录的 `work/codex-quota-resume/state.json`，必要时创建目录。保存实际路径供计划提示词使用；不要把运行状态写入可分享的技能目录。
5. 通过自动任务工具安排一次目标时间的触发，并保存返回的自动任务 id。使用当前工具实际支持的单次调度格式，正确处理年月日、时区和跨日。若使用有限次数的日历规则，核对锚点及下一次触发确实是目标日期；不能只写时分而忽略日期，不能假设所有版本对 `COUNT=1` 的续订语义相同。
6. 读取回显或保存的任务配置，核验启用状态、目标对话和时间规则；如宿主提供下一次触发时间，核对它。只能核验配置时明确说明。宿主不能表达或续订单次触发时，说明限制，不擅自改成每小时或每日轮询。
7. 自动任务提示词必须包含下方“每次运行”完整职责、任务范围、缓冲时间、实际状态路径和本技能已安装的位置，并要求复用原自动任务。不能只写“继续”或依赖最初对话永远保留全部细节。

## 计算检查时间

优先读取 `rateLimitsByLimitId` 中目标任务适用的 Codex 配额，旧版再使用 `rateLimits`。保留实际窗口信息，不将不同模型的独立配额混合。`usedPercent` 是已用比例，剩余为 `max(0, 100-usedPercent)`；`resetsAt` 为 Unix 秒，先转换为绝对时间再按用户时区展示。

- 任一适用窗口耗尽：取所有耗尽窗口中最晚的有效 `resetsAt`，加五分钟。周额度耗尽时等待周恢复，不在中间的五小时窗口反复检查。
- 两个窗口均可用：取五小时窗口的下一次实际 `resetsAt`，加五分钟。不声称剩余额度足以完成整个任务。
- 缺失、旧的已过期重置时间或查询失败：本次至多再读取一次。仍无有效时间时只安排一次三十分钟后的异常复查，并记录原因；连续三次异常通知用户并停止自动重试。这是异常处理，不是常态轮询。
- 不用“现在加五小时”、截图日期或按周机械相加代替实际值。账户未提供可用的新窗口时间时，不编造时间。

## 每次运行

1. 读取状态、当前时间及最新账户额度。计算下一次检查，**先通过自动任务工具更新同一计划并核验，再处理候选任务**。更新完整字段并保持启用；不得因为旧的单次计划已经触发就漏掉续订。记录计划更新结果，失败时不能写成成功。
2. 如果额度仍耗尽，保存状态并结束本次运行。额度可用时列出范围内任务，读取近期候选任务的最新消息及必要错误输出。只处理有明确额度限制错误、未完成目标且没有后来完成或取消证据的任务。`idle`、`notLoaded`、一般失败或长时间没有更新均不构成证据。排除正在运行、主动暂停、等待信息、登录或批准的任务。
3. 每次最多派发一个任务，优先最近因额度中断的任务。如果本监督已派发的任务仍在运行，先不派发第二个。不要更改模型、兑换额度重置券或购买额外额度。
4. 发送前再次核对原任务状态与最新消息。按任务 id 加额度错误的回合 id 或可验证时间去重。先保存 `pending`，成功发送后保存 `sent`。结果不确定时读取原任务历史确认送达；无法确认则保留不确定状态并说明，不盲目重发。同一次中断仅派发一次，后来新的额度中断可在新一轮恢复后处理。
5. 在原任务发送下方续跑消息。用一次即时快照核验结果，不在监督对话长时间等待。完成则标记完成；没有候选任务也保留已安排的下一次重置检查。
6. 保存最小状态：自动任务 id、监督对话 id、时区、范围、检查时间、窗口数据、下一次目标时间、调度核验结果、连续失败次数，以及派发任务/中断标识/状态/时间。不保存账户标识、认证信息或全文聊天。状态损坏时先从计划和原任务消息核对恢复，不盲目重发。
7. 没有候选任务、等待额度或状态未变化时保持安静；完成、持续失败或确需用户行动时通知，同一未变化问题不重复通知。通知偏好通过宿主对应设置保存，遵循用户选择。

### 续跑消息

按原任务语言发送，可使用：

> 继续完成本任务原先约定的目标，以最新用户要求和验收标准为准。先读取已有 PROGRESS.md（如有），核对实际文件和运行状态，从最近未完成步骤继续。在已有授权范围内自主决定实施方法，解决常规问题并完成必要验证。每完成一个阶段，简要更新工作目录中的 PROGRESS.md，记录完成项、验证结果、下一步及阻塞原因。优先直接工具、文件操作和命令行；确需界面操作时使用可用的 computer use 能力。避免重复规划、重复验证及扩大范围。验收通过后停止。再次额度受限时尽可能保存进度；缺少必须由用户提供的信息时明确说明。此消息不扩大原授权，也不撤销用户暂停或取消指令。

不要强制安装其他技能。阶段检查点在执行中持续保存，不能依赖额度耗尽后才补写。

## 查看与停止

查看时读取实际计划与状态，区分“配置已保存”“已触发”“已成功续跑”，不可混为一谈。用户要求停止时暂停匹配自动任务并核验，不删除工作文件或取消其他任务。

## 分享边界

安装本技能不会自动获得授权或启动监控。接收者提出启用请求后，在其环境发现工具并创建自己的计划。分享包仅含技能和使用说明，不包含任何个人路径、对话 id、账户额度快照、自动任务配置或运行状态。
